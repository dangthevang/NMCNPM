package GUI;

import javax.swing.*;

class UserSelectionMessage extends JFrame {
    private JPanel panel1;
    UserSelectionMessage(){
        add(panel1);
        setSize(400,200);
        setTitle("Nhắc nhở");
        setLocationRelativeTo(null);
    }
}
